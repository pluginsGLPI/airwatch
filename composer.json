{
    "require": {
        "php": "^7.2",
        "ext-curl": "*"
    },
    "require-dev": {
        "glpi-project/tools": "^0.4"
    },
    "config": {
        "optimize-autoloader": true,
        "platform": {
            "php": "7.2.0"
        },
        "sort-packages": true
    }
}
